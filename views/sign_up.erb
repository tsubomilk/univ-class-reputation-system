<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- レスポンシブ対応のためのビューポート設定 -->
  <title>授業口コミ新規会員登録</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>
<body>
<div class="d-flex justify-content-center align-items-center" style="min-height: 100vh;">
  <div class="container">
    <h2 class="card-title text-center mb-4">新規会員登録</h2>
    <div class="card" style="max-width: 650px; margin: auto;">
      
      <div class="card-body">
        <form action="/signup" method="post" class="d-flex flex-column">
          <div class="px-5">
            <div class="form-group">
              <label for="inputID">名前</label>
              <input type="text" class="form-control" id="inputID" name="name" placeholder="名前を入力" required>
            </div>
          <div class="form-group">
            <label for="inputPassword">PASSWORD</label>
            <input type="password" class="form-control mb-2 <%= 'is-invalid' if @error && !params[:password].empty? %>" id="inputPassword" name="password" placeholder="パスワードを入力" required>
            <input type="password" class="form-control <%= 'is-invalid' if @error && !params[:password_confirmation].empty? %>" name="password_confirmation" placeholder="パスワードをもう一度入力" required>
            <% if @error %>
              <div class="invalid-feedback">
                <%= @error %>
              </div>
            <% end %>
          </div>
          </div>
          <div class="text-center mt-2">
            <button type="submit" class="btn mb-2">登録</button>
          </div>
        </form>
      </div>
    </div>
    <div class="text-center mt-3">
      <a href="/signin">戻る</a>
    </div>
  </div>
</div>
<script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
</body>
</html>
